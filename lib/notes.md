# 139

In expenses.dart wird die screen width und height ermittelt
    debugPrint(MediaQuery.of(context).size.width.toString());
    debugPrint(MediaQuery.of(context).size.height.toString());

In Abhaengigkeit von der width werden entweder Column oder Row für die Darstellung ausgewaehlt

## 141

in new_Expends.dart geht die Maske nicht auf volle Breite. Konnte nocht nicht geloest werden
